#ifndef HEAPSORT_H
#define HEAPSORT_H

// ordena o vetor utilizando Heap Sort (max heap)
void heapSort(int vetor[], int tamanho);

// imprime o vetor na tela
void exibirVetor(int vetor[], int tamanho);

#endif
